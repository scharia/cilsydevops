#!/bin/bash

#scipt mencari keyword "shadow"
scannedfile=`ls /etc`
#scannedfile=`ls .`
counter=1

counter=0; for value in $(cat group | grep shadow); do ((counter++)); done; if [ $counter -gt 10 ]; then echo "Lebih dari 10"; else echo "Kurang dari 10"; fi;
